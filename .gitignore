# Python / Django
__pycache__/
*.py[cod]
*.sqlite3
.env
venv/
.venv/

# Node (Tailwind build dependencies)
node_modules/

# NOTE: static/css/tailwind.css is intentionally NOT ignored.
# It is the compiled CSS that must be committed and pushed so it exists on
# the server (PythonAnywhere) without needing a Node build step there.
