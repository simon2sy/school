# Python / Django
__pycache__/
*.py[cod]
*.sqlite3
.env
venv/
.venv/

# Node (Tailwind build dependencies)
node_modules/

# NOTE: static/css/tailwind.css is intentionally NOT ignored — it is the
# compiled CSS that must be committed and pushed so the server (e.g.
# PythonAnywhere) can serve it without a Node build step.

# Static build output (regenerated on the server via `collectstatic`)
staticfiles/

